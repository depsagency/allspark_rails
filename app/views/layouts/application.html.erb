<!DOCTYPE html>
<html lang="en" class="scrollbar-rounded h-full">
  <head>
    <%= render "layouts/head" %>
  </head>
  <body class="h-full" data-controller="theme" data-theme-default-value="light">
    <div class="drawer h-full">
      <input id="my-drawer" type="checkbox" class="drawer-toggle" />
      <div class="drawer-content h-screen flex flex-col">
        <%= render "layouts/header" %>
        
        <%# Impersonation banner - show when impersonating %>
        <%= render ImpersonationBannerComponent.new(
          current_user: current_user,
          impersonating: impersonating?,
          original_user: current_impersonator
        ) if user_signed_in? %>
        
        <%# Flash messages %>
        <div class="container mx-auto mt-4">
          <% flash.each do |type, message| %>
            <% auto_dismiss = (type.to_s == 'notice' || type.to_s == 'success') ? 5000 : 0 %>
            <div class="alert alert-<%= flash_class(type) %> mb-4" 
                 data-controller="alert" 
                 data-alert-auto-dismiss-value="<%= auto_dismiss %>"
                 data-alert-animation-value="fade">
              <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
                <% if type == 'notice' || type == 'success' %>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                <% elsif type == 'alert' || type == 'error' %>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
                <% elsif type == 'warning' %>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
                <% else %>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                <% end %>
              </svg>
              <span><%= message %></span>
              <button data-action="click->alert#dismiss" class="btn btn-sm btn-ghost ml-auto">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
              </button>
            </div>
          <% end %>
        </div>
        
        <div class="<%= container_class %> mx-auto mt-4 flex-grow">
          <%= yield %>
        </div>
        <%= render "layouts/footer" %>
      </div>
      <%= render "layouts/sidebar" %>
    </div>
    <%= render "layouts/analytics" %>
  </body>
</html>
